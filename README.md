# Report Source

The main report source is `assignment2_report.tex`.

It is written in IEEE two-column conference style using:

- `\documentclass[10pt,conference]{IEEEtran}`

To compile on a machine with LaTeX installed:

```bash
pdflatex assignment2_report.tex
pdflatex assignment2_report.tex
```

This workspace does not currently have `pdflatex` installed, so the PDF was not compiled here.
